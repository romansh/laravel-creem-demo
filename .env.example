TRAEFIK_HOST=localhost
APP_NAME="Laravel Creem Demo"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://${TRAEFIK_HOST}

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

VITE_APP_NAME="${APP_NAME}"

# ============================================================================
# Creem.io Payment Provider Configuration
# ============================================================================
# Get your API credentials from: https://www.creem.io/dashboard/developers
#
# IMPORTANT: Configure via web interface (no .env editing needed!)
# 1. Start the app: php artisan serve  or  docker-compose up -d
# 2. Visit: /creem-demo
# 3. Enter API keys in the configuration form
# 4. Click "Save" - stored in session
#
# .env values below are OPTIONAL and only pre-fill the web form
# ============================================================================

CREEM_API_KEY=
CREEM_WEBHOOK_SECRET=

# ============================================================================
# Cloudflare Tunnel Configuration (Optional - for Webhook Support)
# ============================================================================
# Cloudflare Tunnel provides secure public access to your local app
# Required for receiving webhooks from Creem.io during local development
#
# Setup Instructions:
# 1. Go to: https://one.dash.cloudflare.com/
# 2. Navigate to: Networks → Tunnels (or Access → Tunnels)
# 3. Click: Create a tunnel
# 4. Choose: Named tunnel (persistent) or Quick tunnel (temporary)
# 5. Select connector: Docker
# 6. Copy tunnel token from the docker run command shown
#    Example: docker run cloudflare/cloudflared:latest tunnel run --token eyJhIjoiXXX...
#    Copy this part: eyJhIjoiXXX...
# 7. Configure public hostname:
#    - For named tunnel: your-app.yourdomain.com
#    - For quick tunnel: *.trycloudflare.com
# 8. Set service: HTTP, URL: laravel.test:80 (for Docker) or localhost:8000 (for artisan serve)
# 9. Paste values below:
# ============================================================================

CLOUDFLARED_TUNNEL_TOKEN=
CLOUDFLARED_TUNNEL_DOMAIN=

# Examples:
# CLOUDFLARED_TUNNEL_TOKEN=eyJhIjoiYourActualTokenHere...
# CLOUDFLARED_TUNNEL_DOMAIN=creem-demo.yourdomain.com
#
# After configuring:
# - Restart Docker: docker-compose down && docker-compose up -d
# - Check logs: docker-compose logs cloudflared
# - Configure webhook in Creem.io: https://your-domain.com/creem/webhook
#
# Alternative to Cloudflare Tunnel: Use ngrok (simpler but temporary URLs)
# ============================================================================
